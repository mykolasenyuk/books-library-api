const getAllBooks = require('./getAllBooks')
const addBook = require('./addBook')
const dltBook = require('./dltBook')
const updBook = require('./updateBook')
const getBookById= require('./getBookById')

module.exports = {
  getAllBooks,
  addBook,
    dltBook,
    updBook,
  getBookById
}
